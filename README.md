# SureCheck MultiDx — Prototype

## Run locally
npm install
npm run dev
(opens at http://localhost:5173)

## Deploy to a public URL
Easiest: drag this whole folder into https://app.netlify.com/drop after running `npm run build` (deploy the generated `dist` folder), or push to GitHub and import into https://vercel.com/new.
